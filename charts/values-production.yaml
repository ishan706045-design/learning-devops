replicaCount: 2

image:
  repository: "asia-south1-docker.pkg.dev/learning-481011/learning/myapp"
  pullPolicy: Always
  # imagePullSecret: regcred
  tag: ""   # overwritten by CI

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  host: ""   # no domain now

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "100m"
    memory: "128Mi"

readinessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 10
  failureThreshold: 3

livenessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 15
  periodSeconds: 20

env:
  - name: PORT
    value: "8080"
  - name: JWT_SECRET
    valueFrom:
      secretKeyRef:
        name: prod-pod-env-secret
        key: JWT_SECRET

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 60
